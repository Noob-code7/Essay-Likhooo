---
phase: 03-background-ai-evaluation-service
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "package.json"
  - ".env.example"
  - "supabase/migrations/20260609_add_retry_count.sql"
  - "scripts/worker.ts"
autonomous: true
requirements: ["AI-01", "AI-02", "AI-03"]

must_haves:
  truths:
    - "Worker successfully processes pending submissions, updates status to completed, and creates ai_scores records"
    - "Gemini API returns structured JSON containing scores (grammar, accuracy, quality, overall) and constructive feedback"
    - "AI evaluation service runs asynchronously and polls database without blocking client requests"
    - "Failed evaluations are retried 3 times before setting status to failed"
  artifacts:
    - path: "supabase/migrations/20260609_add_retry_count.sql"
      provides: "Database migration adding retry_count column to submissions table"
    - path: "scripts/worker.ts"
      provides: "Asynchronous background processor polling database and calling Gemini API"
---

<objective>
Implement the asynchronous background worker service to queue and evaluate student submissions using Google Gemini API or a transparent mock fallback.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Retry Count Database Migration</name>
  <files>supabase/migrations/20260609_add_retry_count.sql</files>
  <read_first>supabase/migrations/20260609_init_schema.sql</read_first>
  <action>Create the SQL migration file 'supabase/migrations/20260609_add_retry_count.sql'. Add an ALTER TABLE command to add a 'retry_count' column of type INTEGER, defaulting to 0, not null, to the submissions table.</action>
  <verify>Check database migration exists and contains ALTER TABLE statement</verify>
  <acceptance_criteria>
    - Migration file contains ALTER TABLE submissions ADD COLUMN retry_count
  </acceptance_criteria>
  <done>Database retry count column added</done>
</task>

<task type="auto">
  <name>Task 2: Install Gemini SDK and Update Environment Configurations</name>
  <files>package.json,.env.example</files>
  <read_first>package.json</read_first>
  <action>Install the Google Gen AI official library using 'npm install @google/generative-ai' (or add it directly to dependencies). Update '.env.example' to add the GEMINI_API_KEY environment variable. If needed, append the dummy api key fallback instructions in .env.example.</action>
  <verify>Confirm dependency is installed and package.json contains @google/generative-ai</verify>
  <acceptance_criteria>
    - package.json contains dependency '@google/generative-ai'
    - .env.example contains GEMINI_API_KEY
  </acceptance_criteria>
  <done>Gemini SDK installed and environment templates configured</done>
</task>

<task type="auto">
  <name>Task 3: Build Asynchronous Background AI Evaluation Worker</name>
  <files>scripts/worker.ts</files>
  <read_first>src/lib/db.ts</read_first>
  <action>Create the file 'scripts/worker.ts'.
  The worker script must:
    - Load environment configuration and check GEMINI_API_KEY.
    - If key is empty or equals 'dummy-api-key', use a transparent mock evaluator (simulates evaluation with a 2-second sleep delay, generating realistic randomized scores and detailed prompt feedback text).
    - If key is configured, use `@google/generative-ai` to evaluate the essay against the exam topic, specifying structured JSON response output.
    - Run an infinite polling loop (every 5 seconds) searching for submissions where status is 'pending'.
    - For each pending submission:
      - Update status to 'evaluating'.
      - Run evaluation to generate grammarScore (0-30), accuracyScore (0-30), qualityScore (0-30), overallScore (0-100), and feedback.
      - Insert the results into 'ai_scores'.
      - Transition status to 'completed'.
      - Implement retry tracking: on failure, increment 'retry_count'. If count is less than 3, return status to 'pending' for retry. If count is 3, set status to 'failed'.
  Add the worker run command as `"worker": "tsx scripts/worker.ts"` script inside 'package.json'.</action>
  <verify>Launch worker in background, verify it polls database and evaluates test submissions successfully</verify>
  <acceptance_criteria>
    - scripts/worker.ts uses isMockAi fallback when GEMINI_API_KEY is not configured
    - scripts/worker.ts queries submissions with status 'pending'
    - scripts/worker.ts inserts score rows with grammar, accuracy, quality, and overall scores matching tables
    - package.json contains script 'worker'
  </acceptance_criteria>
  <done>Background AI evaluation worker complete</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 'npm run build' completes successfully with zero compile errors
- [ ] Running worker processes a pending submission from mock database and successfully updates status to 'completed' and inserts score reports
- [ ] AI scores and feedback are correctly saved with the expected bounds
</verification>

<success_criteria>
- All tasks completed
- Build compiles cleanly
- Verification checks confirm worker asynchronously grades drafts
</success_criteria>

<output>
After completion, create `.planning/phases/03-background-ai-evaluation-service/03-01-SUMMARY.md`
</output>
