---
phase: 02-student-examination-flow
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - "supabase/migrations/20260609_add_draft_status.sql"
  - "src/app/api/submissions/active/route.ts"
  - "src/app/api/submissions/auto-save/route.ts"
  - "src/app/api/submissions/submit/route.ts"
  - "src/app/write/page.tsx"
  - "src/app/page.tsx"
  - "src/app/submit-success/page.tsx"
autonomous: true
requirements: ["EXAM-03", "EXAM-04", "EXAM-05", "SUB-01", "SUB-02"]

must_haves:
  truths:
    - "Drafts auto-save to the database every 30 seconds and update the UI status indicator"
    - "Submissions with invalid word count (<100 or >2000) are blocked with a warning"
    - "Final submissions are locked on the client and backend, preventing further changes"
    - "Student is redirected to /submit-success and dashboard redirects to success page if exam is submitted"
  artifacts:
    - path: "supabase/migrations/20260609_add_draft_status.sql"
      provides: "Database migration for draft status and student/exam uniqueness constraint"
    - path: "src/app/api/submissions/active/route.ts"
      provides: "Endpoint fetching student's active submission/draft"
    - path: "src/app/api/submissions/auto-save/route.ts"
      provides: "Endpoint for autosaving student draft"
    - path: "src/app/api/submissions/submit/route.ts"
      provides: "Endpoint for final submission lock"
    - path: "src/app/submit-success/page.tsx"
      provides: "Submission success screen showing pending evaluation status"
---

<objective>
Implement draft autosaving, submission validation, backend/frontend locking, and the submission success landing page.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-student-examination-flow/02-RESEARCH.md
@.planning/phases/02-student-examination-flow/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Draft Status and Uniqueness DB Migration</name>
  <files>supabase/migrations/20260609_add_draft_status.sql</files>
  <read_first>supabase/migrations/20260609_init_schema.sql</read_first>
  <action>Create the file 'supabase/migrations/20260609_add_draft_status.sql'. Add SQL commands to: drop the existing status check constraint on the submissions table; add a new check constraint allowing 'draft', 'pending', 'evaluating', 'completed', and 'failed'; set the default status to 'draft'; and add a unique constraint on (student_id, exam_id) columns. Run the SQL statements or ensure the mock database fallback adapts by allowing these states.</action>
  <verify>Check that SQL file exists and contains the correct ALTER TABLE commands.</verify>
  <acceptance_criteria>
    - Migration file contains check constraint for 'draft'
    - Migration file contains unique constraint submissions_student_exam_unique
  </acceptance_criteria>
  <done>Database draft status migration defined</done>
</task>

<task type="auto">
  <name>Task 2: Build Submissions GET Active API Endpoint</name>
  <files>src/app/api/submissions/active/route.ts</files>
  <read_first>src/lib/db.ts</read_first>
  <action>Create 'src/app/api/submissions/active/route.ts'. Extract student session cookie, verify JWT. Fetch active exam and then query submissions table for matching (student_id, exam_id). Return the submission object if found, or null if not.</action>
  <verify>Build completes cleanly and GET request returns 200 OK</verify>
  <acceptance_criteria>
    - Endpoint returns 401 if unauthorized
    - Endpoint returns 200 with submission object or null for authorized student
  </acceptance_criteria>
  <done>GET active submission endpoint implemented</done>
</task>

<task type="auto">
  <name>Task 3: Build Submissions Autosave and Submit Post API Endpoints</name>
  <files>src/app/api/submissions/auto-save/route.ts,src/app/api/submissions/submit/route.ts</files>
  <read_first>src/lib/db.ts</read_first>
  <action>Create 'src/app/api/submissions/auto-save/route.ts' and 'src/app/api/submissions/submit/route.ts'. 
  Both endpoints must:
    - Verify student session cookie.
    - Check if submission exists. If status is NOT 'draft', return 403 Forbidden.
  Autosave route:
    - Parse body for essayText.
    - Upsert submission: status = 'draft', essay_text = essayText, word_count = count.
  Submit route:
    - Parse body for essayText.
    - Validate word count is >= 100 and <= 2000 words.
    - Update submission: status = 'pending', essay_text = essayText, word_count = count, submitted_at = now().</action>
  <verify>Mock DB matches updated data and locks further submissions</verify>
  <acceptance_criteria>
    - Auto-save updates database row and preserves 'draft' status
    - Submit validates length, sets status to 'pending', and returns success
    - Calling auto-save or submit on a 'pending' submission returns 403 Forbidden
  </acceptance_criteria>
  <done>Autosave and final submit endpoints created and secured</done>
</task>

<task type="auto">
  <name>Task 4: Build Submission Success Landing Page</name>
  <files>src/app/submit-success/page.tsx</files>
  <read_first>src/app/page.tsx</read_first>
  <action>Create the file 'src/app/submit-success/page.tsx'. Retrieve student session, verify authorization. Display exam topic and student information. Render a card with green success badge and an animated/pulsing 'Pending Evaluation' status. Add a 'Back to Dashboard' button linking to '/'. Style following slate aesthetic.</action>
  <verify>Page mounts correctly and shows pending evaluation details</verify>
  <acceptance_criteria>
    - Renders heading 'Submission Successful'
    - Shows status badge 'Pending Evaluation'
  </acceptance_criteria>
  <done>Submission success page implemented</done>
</task>

<task type="auto">
  <name>Task 5: Integrate Client-side Autosave and Submit Action</name>
  <files>src/app/write/page.tsx</files>
  <read_first>src/app/write/page.tsx</read_first>
  <action>Update 'src/app/write/page.tsx' client component.
  1. Fetch active draft on mount. If active draft is already submitted (status != 'draft'), redirect to '/submit-success'. If draft text exists, populate the editor state.
  2. Implement 30-second interval autosave. Compare current text with last saved text. Show save indicator: 'Saving draft...', 'Draft saved at HH:MM:SS', or error state.
  3. Implement submit action: show a confirmation dialog using a custom modal or browser confirm (dialog styling preferred per UI-SPEC). If confirmed, call POST /api/submissions/submit, clear local states, and redirect to '/submit-success'.
  4. Ensure text area is disabled/locked if submitted.</action>
  <verify>Typing changes save dynamically and clicking submit locks editor</verify>
  <acceptance_criteria>
    - Autosave triggers only when changes occur and updates status indicator
    - Submit dialog warns about locking content
    - Success redirect works and disables text inputs
  </acceptance_criteria>
  <done>Client-side autosave, validation, and locking logic integrated</done>
</task>

<task type="auto">
  <name>Task 6: Update Student Dashboard Guard</name>
  <files>src/app/page.tsx</files>
  <read_first>src/app/page.tsx</read_first>
  <action>Modify 'src/app/page.tsx' to query the submissions table for the logged-in student and active exam. If a submission exists and its status is NOT 'draft' (i.e. 'pending', 'evaluating', 'completed', 'failed'), redirect the student directly to '/submit-success'.</action>
  <verify>Dashboard redirects to /submit-success if already submitted</verify>
  <acceptance_criteria>
    - Logged-in student with a submitted exam is redirected to /submit-success on home dashboard access
  </acceptance_criteria>
  <done>Student dashboard guard active</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 'npm run build' completes successfully with zero compile errors
- [ ] Auto-save updates the mock database file successfully
- [ ] Lockouts prevent editing/resubmitting
</verification>

<success_criteria>
- All tasks completed
- Verification checklist passes
- Page builds and runs smoothly on local development environment
</success_criteria>

<output>
After completion, create `.planning/phases/02-student-examination-flow/02-02-SUMMARY.md`
</output>
