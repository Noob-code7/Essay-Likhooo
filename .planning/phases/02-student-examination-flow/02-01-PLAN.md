---
phase: 02-student-examination-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/app/write/page.tsx"
autonomous: true
requirements: ["EXAM-01", "EXAM-02"]

must_haves:
  truths:
    - "Logged-in student can navigate to /write and view the assigned essay topic"
    - "Essay writer page renders a large text writing area, active word counter, and character counter"
  artifacts:
    - path: "src/app/write/page.tsx"
      provides: "Student essay writing page UI"
---

<objective>
Build the Student Essay Writer user interface at '/write'.

Purpose: Provide students with a distraction-free writing environment that displays the exam topic, instructions, word count, character count, and save indicators.
Output: Student essay writing UI component.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Essay Writer Page Skeleton</name>
  <files>src/app/write/page.tsx</files>
  <read_first>src/app/page.tsx</read_first>
  <action>Create the file 'src/app/write/page.tsx'. Check session authorization on server (using cookies & verifyJWT). If unauthenticated, redirect to '/login'. Render a clean heading 'Essay Examination', display the active exam topic fetched from the database, and show instructions (e.g. min 100, max 2000 words).</action>
  <verify>Build runs successfully and /write route exists</verify>
  <acceptance_criteria>
    - write/page.tsx redirects to /login if no cookie or invalid token
    - Page renders the active exam title and topic
  </acceptance_criteria>
  <done>Writing page structure created</done>
</task>

<task type="auto">
  <name>Task 2: Build Editor Text Area and Stats Counters</name>
  <files>src/app/write/page.tsx</files>
  <read_first>src/app/write/page.tsx</read_first>
  <action>Inside 'src/app/write/page.tsx', implement a client-side component (using 'use client') containing a large textarea for essay input. Implement a real-time reactive counter for word count and character count. Implement form validation styling: if word count is less than 100 or greater than 2000, display a warning state. Style using Tailwind slate palette and rounded cards.</action>
  <verify>Ensure word count updates reactively when typing in the textarea</verify>
  <acceptance_criteria>
    - Textarea accepts multi-line input and spans at least 15 rows
    - Word counter splits on whitespace and correctly ignores double spaces/newlines
    - Character counter displays total string length
    - Warning indicator appears when word count is outside 100-2000 range
  </acceptance_criteria>
  <done>Textarea editor and text counters integrated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 'npm run build' completes successfully with zero compile errors
- [ ] /write page successfully displays the exam topic and updates word/character counts reactively
</verification>

<success_criteria>
- All tasks completed
- UI components render correctly on desktop and mobile screens
</success_criteria>

<output>
After completion, create `.planning/phases/02-student-examination-flow/02-01-SUMMARY.md`
</output>
