---
phase: 01-foundation-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "package.json"
  - "tsconfig.json"
  - "tailwind.config.ts"
  - "supabase/migrations/20260609_init_schema.sql"
  - "src/lib/db.ts"
  - "src/lib/auth-helpers.ts"
  - "src/app/layout.tsx"
  - "src/app/page.tsx"
  - "src/app/login/page.tsx"
  - "src/app/admin/login/page.tsx"
  - "src/app/api/auth/student-login/route.ts"
  - "src/app/api/auth/admin-login/route.ts"
  - "src/app/api/auth/logout/route.ts"
  - "scripts/seed.ts"
autonomous: false
requirements: ["AUTH-01", "AUTH-02"]
user_setup:
  - service: supabase
    why: "Supabase Local Emulator or Cloud Database setup for essay platform storage"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase project settings -> API URL"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase project settings -> service_role API key"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase project settings -> anon public API key"
      - name: JWT_SECRET
        source: "Generate secure 32-character random string"

must_haves:
  truths:
    - "Student can successfully authenticate using student ID and password"
    - "Admin can successfully authenticate using admin username and password"
    - "Supabase schema is created with students, exams, submissions, and ai_scores tables"
  artifacts:
    - path: "supabase/migrations/20260609_init_schema.sql"
      provides: "Database schema creation"
    - path: "src/lib/db.ts"
      provides: "Supabase client initialization"
    - path: "src/app/login/page.tsx"
      provides: "Student login UI"
    - path: "src/app/admin/login/page.tsx"
      provides: "Admin login UI"
  key_links:
    - from: "src/app/login/page.tsx"
      to: "src/app/api/auth/student-login/route.ts"
      via: "form action or fetch request"
    - from: "src/app/admin/login/page.tsx"
      to: "src/app/api/auth/admin-login/route.ts"
      via: "form action or fetch request"
---

<objective>
Initialize Next.js application, configure Supabase database schema, implement secure password hashing, write student and admin API authentication logic, build responsive login screens matching educationTech styling, and seed mock exam/credentials.

Purpose: Create the system foundation and secure access points so students can start exams and admins can monitor submissions.
Output: Initialized Next.js project, SQL schema scripts, Supabase client integrations, authentication endpoint logic, login pages, and seed files.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
@~/.gemini/antigravity/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-authentication/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js Application</name>
  <files>package.json, tsconfig.json, tailwind.config.ts</files>
  <read_first>package.json</read_first>
  <action>Create a new Next.js 14+ application in the root directory (c:\Essay) with TypeScript, Tailwind CSS, ESLint, and src/ directory structure. Run 'npx -y create-next-app@latest ./ --ts --tailwind --eslint --src-dir --app --import-alias "@/*" --use-npm'. Install dependency libraries: '@supabase/supabase-js', 'bcryptjs', 'jose', 'lucide-react', and dev dependencies: '@types/bcryptjs'. Add environment template '.env.example' containing Supabase variables and JWT secret.</action>
  <verify>npm run build runs without TypeScript errors</verify>
  <acceptance_criteria>
    - package.json contains "@supabase/supabase-js", "bcryptjs", and "jose"
    - tailwind.config.ts is present and configured
    - .env.example contains NEXT_PUBLIC_SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY
  </acceptance_criteria>
  <done>Next.js setup complete and dependencies installed</done>
</task>

<task type="auto">
  <name>Task 2: Configure Supabase Database Schema</name>
  <files>supabase/migrations/20260609_init_schema.sql</files>
  <read_first>supabase/migrations/20260609_init_schema.sql</read_first>
  <action>Write database migrations creating tables: 'students' (id uuid primary key, student_id text unique, name text, password_hash text, created_at timestamp default now()), 'exams' (id uuid primary key, title text, topic text, start_time timestamp, end_time timestamp, created_at timestamp default now()), 'submissions' (id uuid primary key, student_id uuid references students(id), exam_id uuid references exams(id), essay_text text, word_count integer, submitted_at timestamp, status text check (status in ('pending', 'evaluating', 'completed', 'failed'))), 'ai_scores' (id uuid primary key, submission_id uuid references submissions(id) on delete cascade, grammar_score integer check (grammar_score between 0 and 30), accuracy_score integer check (accuracy_score between 0 and 30), quality_score integer check (quality_score between 0 and 30), overall_score integer check (overall_score between 0 and 100), feedback text, evaluated_at timestamp default now(), model_version text). Run schema definitions to the database. Use parameterized postgres constraints.</action>
  <verify>Check tables exist in Supabase console or via queries</verify>
  <acceptance_criteria>
    - schema.sql includes students, exams, submissions, and ai_scores tables with check constraints on status and scores
    - All columns parameterized with appropriate types and default times
  </acceptance_criteria>
  <done>Database schema sql generated and run</done>
</task>

<task type="auto">
  <name>Task 3: Initialize DB Client and Authentication Helpers</name>
  <files>src/lib/db.ts, src/lib/auth-helpers.ts</files>
  <read_first>src/lib/db.ts</read_first>
  <action>Initialize the Supabase JS client inside 'src/lib/db.ts' using NEXT_PUBLIC_SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY (use service-role key server-side for admin tasks, anon key for client). Build helper methods in 'src/lib/auth-helpers.ts' to hash passwords (using bcryptjs), sign JWT tokens (using jose), and verify JWT sessions. Implement secure HTTP-only cookies verification.</action>
  <verify>Write test assertions for hashing/verification inside code or test script</verify>
  <acceptance_criteria>
    - db.ts initializes Supabase Client with env checks
    - auth-helpers.ts contains password hashing and verification functions
    - Token generation produces a parseable JWT containing ID and Role claims
  </acceptance_criteria>
  <done>Supabase and Auth helper integrations ready</done>
</task>

<task type="auto">
  <name>Task 4: Implement Login APIs and Routing</name>
  <files>src/app/api/auth/student-login/route.ts, src/app/api/auth/admin-login/route.ts, src/app/api/auth/logout/route.ts</files>
  <read_first>src/app/api/auth/student-login/route.ts</read_first>
  <action>Write API handler routes using Next.js App Router API format. Student-login endpoint searches 'students' by student_id, compares bcrypt password hash, and sets HTTP-only cookie containing student session JWT. Admin-login verifies username/password against admin credentials (configurable via env variables, e.g. ADMIN_USERNAME, ADMIN_PASSWORD_HASH). Logout endpoint deletes HTTP-only cookie.</action>
  <verify>curl or postman calls to /api/auth/student-login returns cookie</verify>
  <acceptance_criteria>
    - /api/auth/student-login returns 200 on valid credentials and sets HTTP-only session cookie
    - /api/auth/admin-login sets session cookie with admin role claim
    - All database queries are fully parameterized through Supabase client
  </acceptance_criteria>
  <done>Authentication endpoints complete</done>
</task>

<task type="auto">
  <name>Task 5: Implement Login UI and Layout</name>
  <files>src/app/layout.tsx, src/app/page.tsx, src/app/login/page.tsx, src/app/admin/login/page.tsx</files>
  <read_first>src/app/login/page.tsx, src/app/layout.tsx</read_first>
  <action>Build Student Login at '/login' and Admin Login at '/admin/login'. Apply Outfit font for headings and Inter for body. Root layout redirects unauthenticated students to '/login'. Style screens with Slate background (#F8FAFC), rounded cards, centered container, blue accent buttons (#2563EB), and copywriting matching UI-SPEC: CTA 'Sign In', clear labels, and errors showing 'Invalid credentials. Please verify your ID and password and try again.'. Render responsive CSS using Tailwind.</action>
  <verify>Build runs and outputs responsive design</verify>
  <acceptance_criteria>
    - /login renders student login form with Student ID and Password fields
    - /admin/login renders admin login form
    - Submit buttons trigger login API routes
    - Forms show error text matches copywriting contract when API returns 401
  </acceptance_criteria>
  <done>Login page layouts complete</done>
</task>

<task type="auto">
  <name>Task 6: Create Database Seeder</name>
  <files>scripts/seed.ts</files>
  <read_first>scripts/seed.ts</read_first>
  <action>Write a node execution script to seed the database with a test exam topic and three test students (ID: STU001, STU002, STU003) with hashed passwords. Insert a sample exam topic: 'The Impact of Artificial Intelligence on Modern Education'.</action>
  <verify>Run seeder script: 'npx ts-node scripts/seed.ts' (or tsx) and verify database populated</verify>
  <acceptance_criteria>
    - Seeder script hashes credentials correctly before insertion
    - Running script inserts students and exam records into database
  </acceptance_criteria>
  <done>Mock exam and students seeded</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Authentication screens and database models on localhost:3000</what-built>
  <how-to-verify>Run the Next.js dev server, browse to http://localhost:3000/login. Attempt logging in with STU001 and correct password. Check that session cookie is set. Navigate to http://localhost:3000/admin/login, verify admin credential login. Test entering invalid credentials, verify exact error messaging.</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 'npm run build' completes successfully with zero TypeScript or build errors
- [ ] Database contains student, exam, submission, and ai_score schemas matching the PRD
- [ ] Login workflows return correct session cookies for both student and admin roles
</verification>

<success_criteria>
- All tasks completed
- Database schema matches design
- Secure student and admin logins are fully operational
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-01-SUMMARY.md`
</output>
