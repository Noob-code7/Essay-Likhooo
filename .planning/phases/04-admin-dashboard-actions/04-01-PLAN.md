---
phase: 04-admin-dashboard-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/app/admin/page.tsx"
  - "src/app/admin/dashboard.tsx"
  - "src/app/api/admin/re-evaluate/route.ts"
  - "src/app/api/admin/export-csv/route.ts"
autonomous: true
requirements: ["ADM-01", "ADM-02", "ADM-03", "ADM-04"]

must_haves:
  truths:
    - "Logged-in administrator can view the dashboard showing total students, submissions, pending, completed, failed, and average overall score"
    - "Submissions table lists student ID, name, status badges, scores, and modal action links"
    - "Admin can click View to open a modal rendering the full essay text and constructive grading feedback"
    - "Admin can click Re-evaluate to clear scores and set status back to pending, queueing it for worker re-processing"
    - "Admin can click Export CSV to generate and download a clean spreadsheet containing all submissions and scores"
  artifacts:
    - path: "src/app/admin/page.tsx"
      provides: "Server component authentication guard for admin dashboard"
    - path: "src/app/admin/dashboard.tsx"
      provides: "Client component for administrative stats, data table, and modal dialogs"
    - path: "src/app/api/admin/re-evaluate/route.ts"
      provides: "API endpoint to trigger manual evaluation retry"
    - path: "src/app/api/admin/export-csv/route.ts"
      provides: "API endpoint to generate and stream results spreadsheet"
---

<objective>
Build the Admin Dashboard summarizing student grades, containing modal essay view triggers, manual re-evaluation actions, and CSV export.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Admin Dashboard Server Component Authorization wrapper</name>
  <files>src/app/admin/page.tsx</files>
  <read_first>src/app/page.tsx</read_first>
  <action>Create the file 'src/app/admin/page.tsx'. Check session authorization on server (using cookies & verifyJWT). If unauthenticated or role is not 'admin', redirect to '/admin/login'. Fetch student accounts, exams, submissions, and ai_scores tables to compute general metrics and retrieve data tables. Pass metrics and submissions list to the child component 'AdminDashboardComponent'.</action>
  <verify>Build runs successfully and /admin route requires admin authentication</verify>
  <acceptance_criteria>
    - admin/page.tsx redirects to /admin/login if unauthorized
    - admin/page.tsx queries database tables and extracts metrics
  </acceptance_criteria>
  <done>Admin authorization wrapper built</done>
</task>

<task type="auto">
  <name>Task 2: Build Admin Dashboard UI Component</name>
  <files>src/app/admin/dashboard.tsx</files>
  <read_first>src/app/write/editor.tsx</read_first>
  <action>Create the file 'src/app/admin/dashboard.tsx'. 
  Implement:
    - Header showing 'AI Essay Examination - Admin Portal' and Sign Out action.
    - Summary Cards grid displaying: Total Students, Submitted Essays, Pending Evaluations, Completed Evaluations, Failed Evaluations, and Average Grade.
    - Table displaying student details (ID, name), status badge (green for completed, yellow for pending/evaluating, red for failed), individual category scores (grammar, accuracy, quality), overall grade, and a 'View Essay' action link.
    - Custom Modal dialog that overlays on click: displays student details, active exam title, full essay response text (scrollable), and constructive AI scoring feedback.</action>
  <verify>Observe metrics cards show the correct counts and clicking View opens modal</verify>
  <acceptance_criteria>
    - Summary cards show statistics matching mock database
    - Table lists student names and correct badges
    - Clicking View Essay displays text in modal overlay
  </acceptance_criteria>
  <done>Admin dashboard UI grid and modal essay viewer integrated</done>
</task>

<task type="auto">
  <name>Task 3: Build Admin Re-Evaluation API Route</name>
  <files>src/app/api/admin/re-evaluate/route.ts,src/app/admin/dashboard.tsx</files>
  <read_first>src/lib/db.ts</read_first>
  <action>Create 'src/app/api/admin/re-evaluate/route.ts'. Verify admin authentication. Extract submissionId. Delete any existing 'ai_scores' entries matching submission_id and update the submissions table: set status to 'pending' and retry_count to 0. 
  Add 'Re-evaluate' triggers to action lists inside 'src/app/admin/dashboard.tsx'. Calling it should trigger a POST fetch to the endpoint and refresh client data.</action>
  <verify>Clicking re-evaluate clears old score entries and changes status back to pending, queueing it for worker pick up</verify>
  <acceptance_criteria>
    - Endpoint returns 401 if not authenticated admin
    - Re-evaluate route removes score details and sets status to pending
    - Integration on dashboard triggers state reload
  </acceptance_criteria>
  <done>Manual evaluation triggers enabled</done>
</task>

<task type="auto">
  <name>Task 4: Build Admin CSV Export API Endpoint</name>
  <files>src/app/api/admin/export-csv/route.ts,src/app/admin/dashboard.tsx</files>
  <read_first>src/lib/db.ts</read_first>
  <action>Create 'src/app/api/admin/export-csv/route.ts'. Verify admin authentication. Fetch all students joined with submissions and scores. Convert data array to a CSV string format, properly escaping text newlines and double quotes. Return response header Content-Type 'text/csv' and Content-Disposition attachment. Add download anchor link triggers in 'src/app/admin/dashboard.tsx'.</action>
  <verify>Clicking Export CSV triggers download of essay_results.csv containing all data columns</verify>
  <acceptance_criteria>
    - API returns CSV formatted stream with correct download headers
    - Export button correctly triggers downloading files in browser
  </acceptance_criteria>
  <done>CSV results exporter integrated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 'npm run build' completes successfully with zero compile errors
- [ ] Export file contains exact headers and escaped essay content
- [ ] Clicking re-evaluate resets statuses correctly
</verification>

<success_criteria>
- All tasks completed
- Build succeeds
- Dashboard updates and API actions trigger successfully
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-dashboard-actions/04-01-SUMMARY.md`
</output>
